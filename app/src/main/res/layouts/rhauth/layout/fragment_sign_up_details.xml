<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
  xmlns:android="http://schemas.android.com/apk/res/android"
  xmlns:app="http://schemas.android.com/apk/res-auto"
  android:layout_width="match_parent"
  android:layout_height="match_parent"
  android:background="@color/background">

  <LinearLayout
    android:id="@+id/sign_up_password_container"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="10dp"
    app:layout_constraintEnd_toEndOf="parent"
    app:layout_constraintStart_toStartOf="parent"
    app:layout_constraintTop_toBottomOf="@+id/sign_in_title">

    <AutoCompleteTextView
      android:id="@+id/sign_up_details_first_name_input"
      android:layout_width="match_parent"
      android:layout_height="40dp"
      android:layout_marginBottom="10dp"
      android:background="@drawable/bar"
      android:drawablePadding="5dp"
      android:hint="First Name"
      android:inputType="textAutoComplete"
      android:paddingStart="10dp"
      android:textColor="@color/input_text"
      android:textSize="12sp" />

    <AutoCompleteTextView
      android:id="@+id/sign_up_details_last_name_input"
      android:layout_width="match_parent"
      android:layout_height="40dp"
      android:layout_marginBottom="10dp"
      android:background="@drawable/bar"
      android:drawablePadding="5dp"
      android:hint="Last Name"
      android:inputType="textAutoComplete"
      android:paddingStart="10dp"
      android:textColor="@color/input_text"
      android:textSize="12sp" />

    <AutoCompleteTextView
      android:id="@+id/sign_up_details_password_input"
      android:layout_width="match_parent"
      android:layout_height="40dp"
      android:layout_marginBottom="10dp"
      android:background="@drawable/bar"
      android:drawablePadding="5dp"
      android:hint="Password"
      android:inputType="textPassword"
      android:paddingStart="10dp"
      android:textColor="@color/input_text"
      android:textSize="12sp" />

    <androidx.appcompat.widget.AppCompatButton
      android:foreground="?android:attr/selectableItemBackground"
      android:id="@+id/sign_up_details_submit_btn"
      android:layout_width="match_parent"
      android:layout_height="40dp"
      android:layout_gravity="center"
      android:background="@drawable/submit_active"
      android:text="Sign Up"
      android:textAllCaps="false"
      android:textColor="@color/submit_text" />
  </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>

